// Tostring(), transforma todos os valores de array em strings separados por virgulas, é semelhante ao join()
// quando usando sem argumentos.

let arr = [16, 7, 2, 23];
;
console.log(arr.toString());